@ id: hp05
@ order: 1040
@ title: 5 A Hilbert Functional Perspective on Neurons
@ subtitle: Hilbert Operator for Progressive Encoding (HOPE) §5
@ source: docs/papers/hope-hilbert/hope-hilbert.md

--- note
## 5 A Hilbert Functional Perspective on Neurons
**Paper:** Hilbert Operator for Progressive Encoding (HOPE) · §5 · pages 7–9 · 1 numbered equation(s), 0 figure(s).

> TODO(author): the *why* of this section in 3–5 lines, then teach each formula below.
> Source markdown: `docs/papers/hope-hilbert/hope-hilbert.md`

--- note
### Unnumbered formula

$$
_{-1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p07_004.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum_{c}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p07_005.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p07_006.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p07_007.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{g}_{i}\in \mathcal{H}_{in}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p08_008.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{f}_{i}\in \mathcal{H}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p08_009.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\otimes
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p08_010.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\cdot ^{\mathcal{H}in}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p08_011.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
= ^{\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p08_012.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
= ^{\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p08_013.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
= E_{x\sim P}\Psi(y_{i})\Psi(y_{j})] \cdot ^{\mathcal{H}out} ^{\mathcal{X}} [
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p08_014.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p08_015.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p08_016.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
= ^{\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p08_017.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p08_018.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p08_019.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{\sqrt}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p08_020.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{in,i}^{≜(\gamma}_{i}^{/}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p08_021.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{f}_{2} \in \mathcal{H}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p09_022.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
∫_{1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p09_023.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
c(\Phi(t)) = c_{0} for 0 \le t\le 1. Then
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p09_024.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
∫_{1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p09_025.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
-^{c0}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p09_026.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
E¤(t) dt= -^{c0}(E-E) = -^{c0} ^{0} ^{E}_{b}^{ba}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p09_027.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
∫_{1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p09_028.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
∫_{1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p09_029.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
s¤(t) dt=^{c0}(s(1) -s(0)) ∝ ^{0} ^{E}_{b}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p09_030.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{f}_{1} \in \mathcal{H}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p09_031.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p09_032.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p09_033.png
Unnumbered formula as rendered in the PDF

--- note
### Equation (3)

$$
K(i, i) = (\gamma^{2} \tag{3}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the equation (3) in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p09_034.png
Equation (3) as rendered in the PDF

--- note
### Unnumbered formula

$$
+ \beta_{i}|\gamma_{i}|\phi
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p09_035.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{i})\Phi
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p09_036.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
|\gamma_{i}|
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p09_037.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
|\gamma_{i}|
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p09_038.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p09_039.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p09_040.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
!
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p09_041.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
|\gamma_{j}| w^{eff}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p09_042.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
|\gamma_{i}| w^{eff}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p09_043.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\rho_{eff} ≜
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p09_044.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
,\kappa≜
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p09_045.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
©­­ «
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p09_046.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
®® ¬
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p09_047.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
©­­ «
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p09_048.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
®® ¬
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p09_049.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sqrt 1 +
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p09_050.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
w^{eff}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p09_051.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
w^{eff}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p09_052.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
1 K(i, j) \approx
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p09_053.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p09_054.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p09_055.png
Unnumbered formula as rendered in the PDF

--- note
**[Recap]** TODO(author): 3 bullets — the claim, the mechanism, the consequence for our own models.

