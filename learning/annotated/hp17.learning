@ id: hp17
@ order: 1160
@ title: C Main Paper Proofs
@ subtitle: Hilbert Operator for Progressive Encoding (HOPE) §C
@ source: docs/papers/hope-hilbert/hope-hilbert.md

--- note
## C Main Paper Proofs
**Paper:** Hilbert Operator for Progressive Encoding (HOPE) · §C · pages 42–49 · 6 numbered equation(s), 0 figure(s).

> TODO(author): the *why* of this section in 3–5 lines, then teach each formula below.
> Source markdown: `docs/papers/hope-hilbert/hope-hilbert.md`

--- note
### Unnumbered formula

$$
∫_{1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p42_208.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
g(\|f_{k}\|). ^{k=1} ^{\mathcal{H}} 3. Partition Invariance: \forall f\in \mathcal{H}, \forall N\in Z_{\ge 1}, E(( f)) = E( f/N, . . . , f/N |{}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p42_209.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
).
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p42_210.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum_{N} Then E(\Phi) =
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p42_211.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\|f_{k}\|. ^{k=1} ^{\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p42_212.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{-1} g(\|f\|_{\mathcal{H}})= \|f\|_{\mathcal{H}} =⇒h\equiv gon Im(g)
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p42_213.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
E(( f)) = h
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p42_214.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
g(\|f_{k}\|). ^{k=1} ^{\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p42_215.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\| f\|_{\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p42_216.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\| f\|_{\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p42_217.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum^{N}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p42_218.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
!
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p42_219.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{-1} )= g
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p42_220.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p42_221.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p42_222.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
= g^{-1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p42_223.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\|f\|_{\mathcal{H}} = E( f/N, . . . , f/N |{}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p42_224.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{k=1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p42_225.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
x
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p42_226.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p42_227.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\forall x\ge 0, \forall N\in Z_{\ge 1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p42_228.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
g(x) = Ng
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p42_229.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum^{N}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p43_230.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum^{N}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p43_231.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
1 E(\Phi) =
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p43_232.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
c\|f_{k}\|_{\mathcal{H}} =
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p43_233.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\|f_{k}\|_{\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p43_234.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{k=1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p43_235.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{k=1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p43_236.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\square
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p43_237.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
∫_{1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p43_238.png
Unnumbered formula as rendered in the PDF

--- note
### Equation (43)

$$
E¤(t) dt^{a}(43) E(\Phi(t)) \tag{43}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the equation (43) in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p43_239.png
Equation (43) as rendered in the PDF

--- note
### Unnumbered formula

$$
\mathcal{J} (\Phi_{a}, \Phi_{b}) =
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p43_240.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{0}-c(\Phi(t))
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p43_241.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
¤\mathcal{J} (t) = -\xi(\Phi(t)) ¤E(t)
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p43_242.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
∫_{1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p43_243.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
∫_{1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p43_244.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{0}-\xi(k\Phi(t)) kE¤(t)dt=
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p43_245.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{0}-\xi(\Phi(t)) E¤(t)dt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p43_246.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
k\xi(k\Phi) E¤(t) = \xi(\Phi) E¤(t) =⇒\xi(k\Phi) = k^{-1}\xi(\Phi)
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p43_247.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
c(k\Phi) = \xi(k\Phi)E(k\Phi) = (k^{-1}\xi(\Phi))(kE(\Phi)) = c(\Phi)
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p43_248.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
∫_{1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p43_249.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
E¤(t) dt E(\Phi(t))
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p43_250.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\mathcal{J} (\Phi_{a}, \Phi_{b}) =
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p43_251.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{0}-c(\Phi(t))
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p43_252.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
∫_{Eb}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p44_253.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
∫_{Ea}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p44_254.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
dE -c(\Phi)
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p44_255.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\mathcal{J} (\Phi_{a}, \Phi_{b}) =
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p44_256.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
= E
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p44_257.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
ln (E_{a}) -ln(E_{b})
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p44_258.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
∫_{Ea}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p44_259.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\mathcal{J} (\Phi_{a}, \Phi_{b}) \ge
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p44_260.png
Unnumbered formula as rendered in the PDF

--- note
### Equation (44)

$$
s¤(t) ¤\mathcal{J}_{proj}(t) ≜c(\Phi(t))(44) E(\Phi(t)) \tag{44}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the equation (44) in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p44_261.png
Equation (44) as rendered in the PDF

--- note
### Unnumbered formula

$$
¤\Phi(t)
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p44_262.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
where s¤(t) =
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p44_263.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\|f\|, giving E¤(t) = \alpha¤(t) \|f\|< 0. ^{i\neq ki\mathcal{H}k\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p44_264.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
¤\Phi(t)
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p44_265.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
= | ¤\alpha(t)| \|f\|= -\alpha¤(t) \|f\|= -E¤(t) _{\mathcal{H}}^{Nk\mathcal{H}} ^{k\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p44_266.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
s¤(t) =
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p44_267.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
Substituting s¤(t) = -E¤(t) into the cost baseline \mathcal{J}¤_{capacity}(t) = -c(\Phi)^{E¤}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p44_268.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{E}yields ¤\mathcal{J} (t) = c(\Phi)^{s¤}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p44_269.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
f_{j}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p44_270.png
Unnumbered formula as rendered in the PDF

--- note
### Equation (45)

$$
E(\Phi(t)) \ge E(\Phi_{b})\forall t\in [0, 1] \tag{45}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the equation (45) in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p44_271.png
Equation (45) as rendered in the PDF

--- note
### Unnumbered formula

$$
f_{p}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p44_272.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\|u\|_{\mathcal{H}} + \|v\|_{\mathcal{H}} \ge \|u+ v\|_{\mathcal{H}}:
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p45_273.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
(1 -t)( f_{i}+ f_{j}) + 2tf_{p}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p45_274.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
E(\Phi(t)) \ge E_{rem} +
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p45_275.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p45_276.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
f_{p}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p45_277.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
To prove E(\Phi(t)) \ge E_{rem} +
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p45_278.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{\mathcal{H}}, we require: (1 -t)( f_{i}+ f_{j}) + 2tf_{p}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p45_279.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
f_{p}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p45_280.png
Unnumbered formula as rendered in the PDF

--- note
### Equation (46)

$$
_{\mathcal{H}}\forall t\in [0, 1] \tag{46}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the equation (46) in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p45_281.png
Equation (46) as rendered in the PDF

--- note
### Unnumbered formula

$$
> ^{\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p45_282.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
(1 -t)(x+ y) + 2tz> z
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p45_283.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{2} +^{2} + ( x yx+ y)E_{rem} x+ y>
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p45_284.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
2E_{rem} + x+ y ^{2} ^{2} (x+ y)(2E_{rem} + x+ y) > x+ y+ (x+ y)E_{rem}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p45_285.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{2} ^{2} ^{2} ^{2} 2E_{rem}(x+ y) + x+ 2xy+ y> x+ y+ E_{rem}(x+ y)
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p45_286.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\Phi_{a}-\Phi ̃_{b}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p45_287.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
∫_{1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p45_288.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
s¤(t) c(\Phi(t))dt _{0}E(\Phi(t))
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p45_289.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\mathcal{J}_{proj}(\Phi_{a}, \Phi_{b}) =
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p45_290.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
∫_{1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p46_291.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
c(\Phi_{a}) \mathcal{J}_{proj}(\Phi_{a}, \Phi_{b}) \le
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p46_292.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
E(\Phi_{b})
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p46_293.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\Phi ̃_{b}-\Phi_{a}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p46_294.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
¤\Phi(t)
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p46_295.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{\mathcal{H}N}=
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p46_296.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\square
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p46_297.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
= \mathcal{J}_{bound}(\Phi_{a}, \Phi_{b}) kE(\Phi_{b})
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p46_298.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p46_299.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\Phi ̃_{b}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p46_300.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{\mathcal{H}N}\to 0.
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p46_301.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\|\Phi_{a}\|_{\mathcal{H}N}-
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p46_302.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
D(\Phi_{a}, \Phi ̃_{b}) =
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p46_303.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\Phi_{a}-\Phi ̃_{b}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p46_304.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\Phi ̃_{b}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p46_305.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{\mathcal{H}N}\ge
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p46_306.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{\mathcal{H}}^{N}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p46_307.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\Phi ̃_{b}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p46_308.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
1 lim(\Phi_{a}, ̃\Phi_{b}) = \|\Phi_{a}\|_{\mathcal{H}N}\ge \sqrt _{E(\Phib)\to 0}^{+} D
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p46_309.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{fp}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p46_310.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{\mathcal{H}}^{\le 2(E}_{rem +}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p46_311.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{\mathcal{H}}^{) = 2E(\Phi}_{b}^{).}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p46_312.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_313.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
f^{(a)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_314.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{k}-f ̃^{(b)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_315.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_316.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{k\in \mathcal{S}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_317.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\mathcal{J}_{bound}(\Phi_{a}, \Phi_{b}) = c(\Phi_{a})
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_318.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_319.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
f^{(a)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_320.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
f^{(a)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_321.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{k}-f ̃^{(b)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_322.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{j}-f ̃^{(b)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_323.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
D(\Phi_{a}, \Phi ̃_{b}) =
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_324.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_325.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
+ ^{\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_326.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_327.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{k\in \mathcal{S}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_328.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{j∉\mathcal{S}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_329.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{j}= f ̃^{(b)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_330.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
f^{(a)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_331.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
f^{(b)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_332.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{\mathcal{H}},
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_333.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum _{\mathcal{H}}+
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_334.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{k\in \mathcal{S}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_335.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{m\in \mathcal{S}}_{new}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_336.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_337.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\|s\psi-f_{i}\|^{2}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_338.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
s\psi-f_{j}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_339.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
+ ^{\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_340.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
E_{a}-\|f_{i}\|_{\mathcal{H}} -^{\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_341.png
Unnumbered formula as rendered in the PDF

--- note
### Equation (50)

$$
(50) + s ^{\mathcal{H}} \tag{50}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the equation (50) in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_342.png
Equation (50) as rendered in the PDF

--- note
### Unnumbered formula

$$
f_{j}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_343.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{\|\psi\|}_{\mathcal{H}}^{=1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_344.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{∗}\Psi(u^{∗}\cdot ̃x) \psi=\sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_345.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
K(u^{∗}, u^{∗})
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_346.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_347.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sqrt^{2}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_348.png
Unnumbered formula as rendered in the PDF

--- note
### Equation (52)

$$
u^{∗}= argmax_{\|u\|2=1} \tag{52}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the equation (52) in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_349.png
Equation (52) as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_350.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum^{out}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_351.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
v^{∗}=
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p47_352.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
f_{j}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_353.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\|s\psi-f_{i}\|^{2}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_354.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
= 2 \|s\psi\|^{2} ^{\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_355.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
+ \|f_{i}\|^{2} ^{\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_356.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
-2 ^{\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_357.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
s\psi-f_{j}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_358.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
+ ^{\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_359.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
+ ^{\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_360.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{2} ^{2\mathcal{H}} = 2s+ \|f_{i}\|
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_361.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
f_{j}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_362.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
-2s ^{\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_363.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
+ ^{\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_364.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
f_{j}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_365.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{\|\psi\|}_{\mathcal{H}}^{=1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_366.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{2}1 = (\alpha\beta)K(u, u) =⇒\alpha\beta=\sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_367.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{2} ^{2} 1 = (\alpha\beta)\|v\|E_{x\sim P} ^{2} ^{\mathcal{X}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_368.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\Psi^{2}(u\cdot ̃x)
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_369.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_370.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\Psi(u\cdot ̃x) \psi= \sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_371.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
* \Psi(u\cdot ̃x) \sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_372.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
+
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_373.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
* \Psi(u\cdot ̃x) \sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_374.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
+
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_375.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_376.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\cdot ̃x) ^{in}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_377.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
=
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_378.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{k\in {i,j}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_379.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_380.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_381.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
1 =\sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_382.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_383.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\cdot ̃x) ^{in}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_384.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_385.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_386.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
E_{x\sim P\mathcal{X}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_387.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{k\in {i,j}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_388.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
*
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_389.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
+
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_390.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
1 v,\sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_391.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_392.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
=
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_393.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{k\in {i,j}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_394.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum Let z(u) =
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_395.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{∗}z(u) v(u) = \|z(u)\|_{2}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_396.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_397.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
K(x, y) = \|y\|_{2} k(\rho)where\rho=
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p48_398.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
K(x, y) = E_{v\sim P\mathcal{V}} [\Psi(x^{T}v)\Psi(\|y\|_{2} ˆy^{T}v)] = \|y\|_{2} E_{v\sim P\mathcal{V}} [\Psi(x^{T}v)\Psi(ˆy^{T}v)]
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p49_399.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p49_400.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
E_{v\sim P\mathcal{V}} [\Psi(x^{T}v)\Psi(ˆy^{T}v)] = E_{u\sim P\mathcal{V}} [\Psi(u_{1})\Psi(\rhou_{1} +
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p49_401.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p49_402.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
1 -\rho^{2}u_{2})]
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p49_403.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
K(x, y) = \|y\|_{2} k(\rho).\square
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p49_404.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{2} (C_{+}XI_{X>0} + C_{-}XI_{X<0})
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p49_405.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{+}E[X^{2}I_{X>0}] + C^{2}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p49_406.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{-}E[X^{2}I_{X<0}]
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p49_407.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
k(1) = E
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p49_408.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p49_409.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{2} (C_{+}I_{X>0} + C_{-}I_{X<0})
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p49_410.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{+}E[I_{X>0}] + C^{2}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p49_411.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
k^{′}(1) = E
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p49_412.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p49_413.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{-}E[I_{X<0}]
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p49_414.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
E[X^{2}] =^{1}and ^{22} E[I_{X>0}] = E[I_{X<0}] =^{1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p49_415.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{-}) and k^{′}(1) =^{1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p49_416.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
(C^{2}+ C^{2} ^{2} ^{+}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p49_417.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{-}). Thus, k(1) = k^{′}(1).\square
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p49_418.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
(C^{2}+ C^{2} ^{2} ^{+}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p49_419.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{+} + C^{2}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p49_420.png
Unnumbered formula as rendered in the PDF

--- note
**[Recap]** TODO(author): 3 bullets — the claim, the mechanism, the consequence for our own models.

