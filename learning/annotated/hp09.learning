@ id: hp09
@ order: 1080
@ title: 9 Balancing Compression and Distortion
@ subtitle: Hilbert Operator for Progressive Encoding (HOPE) §9
@ source: docs/papers/hope-hilbert/hope-hilbert.md

--- note
## 9 Balancing Compression and Distortion
**Paper:** Hilbert Operator for Progressive Encoding (HOPE) · §9 · pages 18–18 · 0 numbered equation(s), 0 figure(s).

> TODO(author): the *why* of this section in 3–5 lines, then teach each formula below.
> Source markdown: `docs/papers/hope-hilbert/hope-hilbert.md`

--- note
### Unnumbered formula

$$
\sum^{K}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p18_158.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum^{K}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p18_159.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
(a^{∗}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p18_160.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{1}, \cdot \cdot \cdot , a^{∗}_{K}) = arg min
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p18_161.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{k=1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p18_162.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{k=1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p18_163.png
Unnumbered formula as rendered in the PDF

--- note
**[Recap]** TODO(author): 3 bullets — the claim, the mechanism, the consequence for our own models.

