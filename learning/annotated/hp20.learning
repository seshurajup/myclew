@ id: hp20
@ order: 1190
@ title: F Derivations for Block Eviction
@ subtitle: Hilbert Operator for Progressive Encoding (HOPE) §F
@ source: docs/papers/hope-hilbert/hope-hilbert.md

--- note
## F Derivations for Block Eviction
**Paper:** Hilbert Operator for Progressive Encoding (HOPE) · §F · pages 56–58 · 2 numbered equation(s), 0 figure(s).

> TODO(author): the *why* of this section in 3–5 lines, then teach each formula below.
> Source markdown: `docs/papers/hope-hilbert/hope-hilbert.md`

--- note
### Unnumbered formula

$$
K(i, j) \approx \mathcal{I}(\rhoˆ_{ij})
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p56_575.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p56_576.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
1 K(i, j) \approx
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p56_577.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p56_578.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p56_579.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p56_580.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{d}\sum^{amb}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p57_581.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum^{Nl}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p57_582.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
f^{(l)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p57_583.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\mathcal{F} (\Phi^{(l)}) ≜
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p57_584.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{i},\mathcal{F} (\mathcal{I}) =
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p57_585.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{i=1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p57_586.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{k=1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p57_587.png
Unnumbered formula as rendered in the PDF

--- note
### Equation (87)

$$
_{(1)(2)} \Phi, \Phi+ \mathcal{F} (\mathcal{I}) . \tag{87}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the equation (87) in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p57_588.png
Equation (87) as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p57_589.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\mathcal{B} = \mathcal{F}_{res}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p57_590.png
Unnumbered formula as rendered in the PDF

--- note
### Equation (88)

$$
\mathcal{B}(t) = \mathcal{F}_{res}(t) + \mathcal{F} (\mathcal{I}) . \tag{88}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the equation (88) in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p57_591.png
Equation (88) as rendered in the PDF

--- note
### Unnumbered formula

$$
_{dt}^{d}E(\Phi(t))
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p57_592.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
∫_{1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p57_593.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{0}-c(\Phi(t))
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p57_594.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum^{Ni}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p57_595.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum^{n}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p57_596.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum^{n}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p57_597.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
E(\Omega) =
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p57_598.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
E(T_{i}) =
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p57_599.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{i=1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p57_600.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{i=1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p57_601.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{j=1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p57_602.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{d(l)} (^{d} _{dt}E(\Phit)) +
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p57_603.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
∫_{1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p57_604.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{dt}E(\mathcal{I})
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p57_605.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{(l)} _{0}-c(\Omega(t))
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p57_606.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{b}) =
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p57_607.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{(l)} E(\Phi(t)) + E(\mathcal{I})
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p57_608.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{d(l)} ( _{dt}E(\Phit))
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p57_609.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
∫_{1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p57_610.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{(l)} _{0}-c(\Phi(t))
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p57_611.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{b}) =
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p57_612.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{(l)} E(\Phi(t)) + E(\mathcal{I})
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p57_613.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{(l)} ^{(l)} _{a}\to 0, c(\Phi(t)) drops from N
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p58_614.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
E^{(l)} 1 +
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p58_615.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
∫_{1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p58_616.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
!
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p58_617.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
-E¤(t) E(t) + E_{identity}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p58_618.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{b}) \le N^{(l)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p58_619.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
dt= N^{(l)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p58_620.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
E^{(l)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p58_621.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\mathcal{J}_{layer}(\Omega^{(l)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p58_622.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{b}) ≜N^{(l)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p58_623.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{1/2} _{2}]
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p58_624.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p58_625.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p58_626.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
E_{x\sim P\mathcal{X}} [\|x_{k}e_{k}\|^{2}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p58_627.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
E_{x\sim P\mathcal{X}} [x^{2}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p58_628.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\|\mathcal{I}_{k}\|_{\mathcal{H}} =
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p58_629.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
=
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p58_630.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{2} ^{2} _{k}] = Var(x_{k}) + (E[x_{k}])= \gamma
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p58_631.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{d}\sum^{amb}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p58_632.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p58_633.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{k=1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p58_634.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
-N^{(l)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p58_635.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
∫_{0}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p58_636.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\mathcal{J}^{(l)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p58_637.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{E}^{(l)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p58_638.png
Unnumbered formula as rendered in the PDF

--- note
**[Recap]** TODO(author): 3 bullets — the claim, the mechanism, the consequence for our own models.

