# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

# TODO(author): the equation (117) in runnable PyTorch, then assert it matches the paper.

# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

# TODO(author): the equation (118) in runnable PyTorch, then assert it matches the paper.

# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.
