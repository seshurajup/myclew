@ id: hp19
@ order: 1180
@ title: E Kernel Formulation
@ subtitle: Hilbert Operator for Progressive Encoding (HOPE) §E
@ source: docs/papers/hope-hilbert/hope-hilbert.md

--- note
## E Kernel Formulation
**Paper:** Hilbert Operator for Progressive Encoding (HOPE) · §E · pages 52–55 · 6 numbered equation(s), 0 figure(s).

> TODO(author): the *why* of this section in 3–5 lines, then teach each formula below.
> Source markdown: `docs/papers/hope-hilbert/hope-hilbert.md`

--- note
### Equation (73)

$$
K(i, j) ≜E_{x\sim P\mathcal{X}} [\Psi(y_{i})\Psi(y_{j})] \tag{73}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the equation (73) in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p52_472.png
Equation (73) as rendered in the PDF

--- note
### Unnumbered formula

$$
= ^{\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p52_473.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
=^{\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p52_474.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{R}^{c}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p52_475.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{\mathcal{H}}_{in}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p52_476.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
= K(i, j)
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p52_477.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{2}\sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p52_478.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\|f_{i}\|_{\mathcal{H}} =
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p52_479.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
y_{i}= (w^{eff}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p52_480.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{in,i}=^{\sqrt\gammai}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p52_481.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{i}^{+\epsilon}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p52_482.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\gamma_{i} E[y_{i}] =\sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p52_483.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\gamma_{i}\mu_{i} _{raw,i}x] + b_{i}=\sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p52_484.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\gamma_{i}\mu_{i} + \beta_{i}-\sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p52_485.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
E[w^{T}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p52_486.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
!
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p52_487.png
Unnumbered formula as rendered in the PDF

--- note
### Equation (77)

$$
Var(y_{i}) = \tag{77}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the equation (77) in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p52_488.png
Equation (77) as rendered in the PDF

--- note
### Unnumbered formula

$$
y_{i}\sim \mathcal{N} (\beta_{i}, \gamma^{2}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p52_489.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
∫_{\infty}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p53_490.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
p(y_{i}) = \mathcal{N} (y_{i}; \beta_{i}, \gamma^{2}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p53_491.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p53_492.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p53_493.png
Unnumbered formula as rendered in the PDF

--- note
### Equation (79)

$$
K(i, i) = (\gamma^{2} \tag{79}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the equation (79) in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p53_494.png
Equation (79) as rendered in the PDF

--- note
### Unnumbered formula

$$
+ \beta_{i}|\gamma_{i}|\phi
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p53_495.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{i})\Phi
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p53_496.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
|\gamma_{i}|
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p53_497.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
|\gamma_{i}|
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p53_498.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
∫_{\infty}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p53_499.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
∫_{\infty}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p53_500.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
∫_{\infty}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p53_501.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
∫_{\infty}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p53_502.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
∫_{\infty}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p53_503.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
(|\gamma_{i}|z+ \beta_{i})^{2}\phi(z)dz= \beta^{2}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p53_504.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\phi(z)dz+ 2\beta_{i}|\gamma_{i}|
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p53_505.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
K(i, i) =
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p53_506.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{-c}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p53_507.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{-c}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p53_508.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{-c}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p53_509.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{-c}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p53_510.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
∫_{\infty} •
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p53_511.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\phi(z)dz= 1 -\Phi(-c) = \Phi(c) ∫^{-}_{\infty}^{c} •
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p53_512.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{-c}z\phi(z)dz= [-\phi(z)]^{\infty}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p53_513.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{-c}= \phi(c) ∫_{\infty} •
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p53_514.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
∫_{\infty}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p53_515.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{-c}z^{2}\phi(z)dz= [-z\phi(z)]^{\infty}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p53_516.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{-c}\phi(z)dz= \Phi(c) -c\phi(c)
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p53_517.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{-c}+
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p53_518.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
K(i, i) = \beta^{2}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p53_519.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{i}\Phi(c) + 2\beta_{i}|\gamma_{i}|\phi(c) + \gamma^{2}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p53_520.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\Phi(c) -c\phi(c)
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p53_521.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p53_522.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
= (\gamma^{2}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p53_523.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{2} _{i})\Phi(c) + 2\beta_{i}|\gamma_{i}| -\gamma
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p53_524.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{i}c
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p53_525.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\phi(c)
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p53_526.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p54_527.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p54_528.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{weff}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p54_529.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{weff}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p54_530.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sqrt 1 +
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p54_531.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
!
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p54_532.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
|\gamma_{j}| w^{eff}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p54_533.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
|\gamma_{i}| w^{eff}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p54_534.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
©­­ «
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p54_535.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
®® ¬
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p54_536.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
©­­ «
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p54_537.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
®® ¬
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p54_538.png
Unnumbered formula as rendered in the PDF

--- note
### Equation (81)

$$
\kappa= \tag{81}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the equation (81) in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p54_539.png
Equation (81) as rendered in the PDF

--- note
### Unnumbered formula

$$
_{weff}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p54_540.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{T-1} ^{-1} ^{T-1T-1} ^{-1T} (I+ W\LambdaW)= I-W(\Lambda+ WW)W= I-W(\Lambda+ G)W
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p54_541.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{-1} ^{-1T} I-W(\Lambda+ G)W
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p54_542.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{-1} ^{-1} W= G-G(\Lambda+ G)G
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p54_543.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{T} C= W
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p54_544.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{-1} ^{-1} I-(\Lambda+ G)G
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p54_545.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p54_546.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p54_547.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{-1} ^{-1-1} ^{-1} ^{-1} (\Lambda+ G)(\Lambda+ G) -(\Lambda+ G)G
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p54_548.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p54_549.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p54_550.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
= G
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p54_551.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{-1} ^{-1-1} = G(\Lambda+ G)\Lambda
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p54_552.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{-1} ^{-1} ^{-1} ^{-1-1} ^{-1} ^{-1} C= \Lambda(\Lambda+ G)G= \Lambda\LambdaG+ \LambdaGG= G+ \Lambda
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p54_553.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{-1}-\rho_{raw} [G]_{12} = w_{raw,i}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p55_554.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{-1}-r_{raw} ,[C]_{12} = (1 -\rho^{2raw})\sigma_{i}\sigma_{j}(1 -r^{2}) ^{2} ^{raw}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p55_555.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p55_556.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
!
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p55_557.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
!
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p55_558.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{in,i}=^{\sqrt\gammai}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p55_559.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{i}^{+\epsilon}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p55_560.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
= \kappa=⇒\kappa\rhoˆ^{2}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p55_561.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{\sqrt}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p55_562.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p55_563.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{2} \gamma
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p55_564.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p55_565.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p55_566.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p55_567.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p55_568.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p55_569.png
Unnumbered formula as rendered in the PDF

--- note
### Equation (82)

$$
\sim \mathcal{N} \tag{82}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the equation (82) in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p55_570.png
Equation (82) as rendered in the PDF

--- note
### Unnumbered formula

$$
"
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p55_571.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
K(i, j) = |\gamma_{i}\gamma_{j}|
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p55_572.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
#
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p55_573.png
Unnumbered formula as rendered in the PDF

--- note
### Equation (83)

$$
+ c_{j}\phi(c_{i})\Phi(c_{j|i}) + (1 -\rhoˆ^{2} \tag{83}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the equation (83) in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p55_574.png
Equation (83) as rendered in the PDF

--- note
**[Recap]** TODO(author): 3 bullets — the claim, the mechanism, the consequence for our own models.

