@ id: hp22
@ order: 1210
@ title: H Theoretical Guarantees of DEFT
@ subtitle: Hilbert Operator for Progressive Encoding (HOPE) §H
@ source: docs/papers/hope-hilbert/hope-hilbert.md

--- note
## H Theoretical Guarantees of DEFT
**Paper:** Hilbert Operator for Progressive Encoding (HOPE) · §H · pages 62–66 · 13 numbered equation(s), 0 figure(s).

> TODO(author): the *why* of this section in 3–5 lines, then teach each formula below.
> Source markdown: `docs/papers/hope-hilbert/hope-hilbert.md`

--- note
### Unnumbered formula

$$
_{2} \Psi(y_{i}(x))
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p62_648.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p62_649.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
K(i, i)whereK(i, i) = E_{x\sim P\mathcal{X}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p62_650.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\|f_{i}\|_{\mathcal{H}} = \|w_{out,i}\|_{2}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p62_651.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p62_652.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{\Delta \neq 0}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p63_653.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{\times}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p63_654.png
Unnumbered formula as rendered in the PDF

--- note
### Equation (103)

$$
^{l)} ^{(l)} _{core} = {i\in \mathcal{I}^{(}| \|f_{i}\|_{\mathcal{H}(l)} > \tau} \tag{103}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the equation (103) in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p63_655.png
Equation (103) as rendered in the PDF

--- note
### Unnumbered formula

$$
\mathcal{N}^{(l)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p63_656.png
Unnumbered formula as rendered in the PDF

--- note
### Equation (104)

$$
^{l)} ^{(l)} = { j\in \mathcal{I}^{(}| \|f_{j}\|_{(l)} \le \tau}(104) ^{slack} ^{\mathcal{H}} \tag{104}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the equation (104) in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p63_657.png
Equation (104) as rendered in the PDF

--- note
### Unnumbered formula

$$
\mathcal{N}^{(l)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p63_658.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
neuron u\in \mathcal{N}^{(l+1)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p63_659.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{slack}, \forall u\in \mathcal{N}^{(l+1)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p64_660.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
I_{u,j}= 0\forall j\in \mathcal{N}^{(l)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p64_661.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum _{core}(x) ≜
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p64_662.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{j\in \mathcal{N}}^{(l)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p64_663.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\Deltas^{init}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p64_664.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\le |\mathcal{N}^{(l)} _{\mathcal{H}}^{(l)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p64_665.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p64_666.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p64_667.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
s_{core}(x) =
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p64_668.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{i\in \mathcal{N}}^{(l)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p64_669.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{j\in \mathcal{N}}^{(l)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p64_670.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_671.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{core}(x) =
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_672.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{j\in \mathcal{N}}^{(l)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_673.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\Deltas^{init}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_674.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_675.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{2}g_{j}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_676.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\le _{\mathcal{H}}^{(l)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_677.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{\mathcal{H}}^{(l)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_678.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{j\in \mathcal{N}}^{(l)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_679.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_680.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
g_{j}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_681.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
= ^{in}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_682.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{\mathcal{H}}^{(l)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_683.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\le w_{out,j} ^{2}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_684.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_685.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_686.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{2}\sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_687.png
Unnumbered formula as rendered in the PDF

--- note
### Equation (111)

$$
\|f_{j}\|_{\mathcal{H}(l)} \tag{111}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the equation (111) in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_688.png
Equation (111) as rendered in the PDF

--- note
### Unnumbered formula

$$
K( j, j) =
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_689.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\le _{\mathcal{H}}^{(l)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_690.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{j\in \mathcal{N}}^{(l)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_691.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{j\in \mathcal{N}}^{(l)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_692.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\Deltas^{init}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_693.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{(l)} ^{(l)} ^{(l)} \tau= \tau|\mathcal{N}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_694.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_695.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\le _{\mathcal{H}}^{(l)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_696.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{j\in \mathcal{N}}^{(l)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_697.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\square
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_698.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
f^{(l),(t)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_699.png
Unnumbered formula as rendered in the PDF

--- note
### Equation (0)

$$
_{i}-f^{(l),(0)} \tag{0}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the equation (0) in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_700.png
Equation (0) as rendered in the PDF

--- note
### Unnumbered formula

$$
\forall l, i\in \mathcal{N}^{(l)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_701.png
Unnumbered formula as rendered in the PDF

--- note
### Equation (113)

$$
= 0(113) _{\mathcal{H}}^{(l)} \tag{113}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the equation (113) in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_702.png
Equation (113) as rendered in the PDF

--- note
### Unnumbered formula

$$
_{i}(x) =
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_703.png
Unnumbered formula as rendered in the PDF

--- note
### Equation (0)

$$
g^{(0)} \tag{0}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the equation (0) in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_704.png
Equation (0) as rendered in the PDF

--- note
### Unnumbered formula

$$
_{i}(x) for all i\in \mathcal{N}^{(l)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_705.png
Unnumbered formula as rendered in the PDF

--- note
### Equation (0)

$$
_{core}(x) -s^{(0)} \tag{0}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the equation (0) in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_706.png
Equation (0) as rendered in the PDF

--- note
### Equation (0)

$$
_{i}(x) -w^{(0)} \tag{0}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the equation (0) in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_707.png
Equation (0) as rendered in the PDF

--- note
### Equation (0)

$$
_{j}(x) -w^{(0)} \tag{0}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the equation (0) in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_708.png
Equation (0) as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_709.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_710.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_711.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_712.png
Unnumbered formula as rendered in the PDF

--- note
### Equation (114)

$$
w^{(t)} \tag{114}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the equation (114) in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_713.png
Equation (114) as rendered in the PDF

--- note
### Unnumbered formula

$$
w^{(t)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_714.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_715.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_716.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\Deltas_{core}(x) =
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_717.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{i}(x)
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_718.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
+
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_719.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{j}(x)
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_720.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{i\in \mathcal{N}}^{(l)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_721.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{j\in \mathcal{N}}^{(l)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_722.png
Unnumbered formula as rendered in the PDF

--- note
### Equation (0)

$$
_{i}(x) = g^{(0)} \tag{0}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the equation (0) in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p65_723.png
Equation (0) as rendered in the PDF

--- note
### Equation (0)

$$
_{u}(x) = g^{(0)} \tag{0}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the equation (0) in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p66_724.png
Equation (0) as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum _{core} ≜
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p66_725.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{u\in \mathcal{N}}^{(l+1)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p66_726.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\Deltas^{init,(l+1)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p66_727.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
|\mathcal{N}^{(l)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p66_728.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum^{L-1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p66_729.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum^{L-1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p66_730.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
!
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p66_731.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{\mathcal{H}}^{(l)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p66_732.png
Unnumbered formula as rendered in the PDF

--- note
### Equation (115)

$$
\le \tag{115}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the equation (115) in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p66_733.png
Equation (115) as rendered in the PDF

--- note
### Unnumbered formula

$$
E^{(l+1)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p66_734.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
E^{(l+1)}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p66_735.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{l=1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p66_736.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{l=1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p66_737.png
Unnumbered formula as rendered in the PDF

--- note
**[Recap]** TODO(author): 3 bullets — the claim, the mechanism, the consequence for our own models.

