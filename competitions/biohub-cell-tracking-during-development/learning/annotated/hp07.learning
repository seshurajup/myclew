@ id: hp07
@ order: 1060
@ title: 7 Generating the Parent Neuron
@ subtitle: Hilbert Operator for Progressive Encoding (HOPE) §7
@ source: docs/papers/hope-hilbert/hope-hilbert.md

--- note
## 7 Generating the Parent Neuron
**Paper:** Hilbert Operator for Progressive Encoding (HOPE) · §7 · pages 13–15 · 3 numbered equation(s), 0 figure(s).

> TODO(author): the *why* of this section in 3–5 lines, then teach each formula below.
> Source markdown: `docs/papers/hope-hilbert/hope-hilbert.md`

--- note
### Unnumbered formula

$$
\|f_{i}-0\|^{2}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p13_072.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
E(\Phi_{b}) = E_{a}-\|f_{i}\|_{\mathcal{H}} we get \mathcal{J}_{prune} =^{N\cdot \|fi\|\mathcal{H}}. ^{Ea-\|fi\|\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p13_073.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p13_074.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p13_075.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
D^{2}(\Phi_{a}, \Phi_{b}) = \|W_{joint} -W^{′}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p13_076.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
= \|f_{i}-f_{p}\|^{2}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p13_077.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
+ \|f_{j}-f_{p}\|^{2} ^{\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p13_078.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\|W_{joint}(x) -W^{′}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p13_079.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
. ^{\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p13_080.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
= E_{x\sim P} ^{\mathcal{H}} ^{\mathcal{X}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p13_081.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p13_082.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\|f_{i}-f_{p}\|^{2}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p13_083.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
+ \|f_{j}-f_{p}\|^{2} ^{\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p13_084.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
N\|f_{i}\|_{\mathcal{H}} \mathcal{J}_{prune} = E_{a}-\|f_{i}\|_{\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p13_085.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
E_{a}-\|f_{i}\|_{\mathcal{H}} -\|f_{j}\|_{\mathcal{H}} + \|f_{p}\|^{\mathcal{H}}_{\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p13_086.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
,\mathcal{J}_{merge} =
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p13_087.png
Unnumbered formula as rendered in the PDF

--- note
### Equation (7)

$$
\mathcal{N} ≜{ f| f(x) = w_{out}\Psi( ̃w_{in} \cdot ̃x)} ⊂\mathcal{H} . \tag{7}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the equation (7) in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p13_088.png
Equation (7) as rendered in the PDF

--- note
### Unnumbered formula

$$
\sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p13_089.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\|f_{p}-f_{i}\|^{2}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p13_090.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
+ \|f_{p}-f_{j}\|^{2} ^{\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p13_091.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
E_{a}-\|f_{i}\|_{\mathcal{H}} -\|f_{j}\|_{\mathcal{H}} + \|f_{p}^{\mathcal{H}}\|_{\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p13_092.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{\psi\in \mathcal{N}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p13_093.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\Psi(u\cdot ̃x) \psi= \sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p14_094.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum \|
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p14_095.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{out}\| \sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p14_096.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p14_097.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum^{out} \|
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p14_098.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
v^{∗}=
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p14_099.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p14_100.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p14_101.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
x,_{\|y}^{y}_{\|}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p14_102.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p14_103.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p14_104.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{\|k}\sqrt^{(1) Au\|}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p14_105.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p14_106.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p14_107.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
+ \|f_{j}\|^{2} ^{\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p15_108.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{\mathcal{H}},
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p15_109.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
b≜
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p15_110.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\|f_{i}\|^{2}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p15_111.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
+ \|f_{j}\|^{2} ^{\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p15_112.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
+ E_{rem} ^{\mathcal{H}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p15_113.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{∗∗∗}\Psi(u_{c} \cdot ̃x) _{p}(x ̃) = s\psi(x ̃),\psi(x ̃) = \sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p15_114.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{\mathcal{H}} 2E_{rem} +
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p15_115.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
f^{∗}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p15_116.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
v^{∗},s^{∗}=
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p15_117.png
Unnumbered formula as rendered in the PDF

--- note
### Equation (12)

$$
^{\mathcal{H}} \tag{12}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the equation (12) in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p15_118.png
Equation (12) as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum \|
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p15_119.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{out}\| \sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p15_120.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p15_121.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sum^{out} \|
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p15_122.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
,v^{∗}=
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p15_123.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
u_{c} = argmax_{u\in {u,-u}}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p15_124.png
Unnumbered formula as rendered in the PDF

--- note
### Equation (14)

$$
. \tag{14}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the equation (14) in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p15_125.png
Equation (14) as rendered in the PDF

--- note
### Unnumbered formula

$$

$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p15_126.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{in})^{T}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p15_127.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
^{\|u\|=1}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p15_128.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{p}(x ̃) = w^{∗}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p15_129.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p15_130.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
w^{∗}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p15_131.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p15_132.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{in}\|_{2}/\|w^{∗}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p15_133.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
= \sqrt ^{in}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p15_134.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
s^{∗}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p15_135.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
\sqrt
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p15_136.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
s^{∗}R_{F}\cdot K^{-1/4}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p15_137.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
w ̃^{∗}
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p15_138.png
Unnumbered formula as rendered in the PDF

--- note
### Unnumbered formula

$$
_{out} =
$$

> TODO(author): what each symbol is, what it *does*, and why the paper needs it.

--- code
# TODO(author): the unnumbered formula in runnable PyTorch, then assert it matches the paper.

--- image
learning/assets/hope-hilbert/eq/eq_p15_139.png
Unnumbered formula as rendered in the PDF

--- note
**[Recap]** TODO(author): 3 bullets — the claim, the mechanism, the consequence for our own models.

